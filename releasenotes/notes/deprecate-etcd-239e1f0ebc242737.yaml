---
deprecations:
  - |
    The etcd driver has been deprecated, because it supports only the v2 API,
    which was removed in etcd 3.6. Use the etcd3gw driver instead, which
    supports the v3 API.
