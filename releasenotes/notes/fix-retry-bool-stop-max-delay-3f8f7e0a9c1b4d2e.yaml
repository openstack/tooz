---
fixes:
  - |
    Fixed a bug where blocking lock acquisition (e.g.
    ``MemcachedLock.acquire()`` with its default ``blocking=True``)
    was incorrectly treated as a 1-second timeout instead of "no
    limit", causing it to raise ``tenacity.RetryError`` after about a
    second instead of waiting for the lock to free up.
